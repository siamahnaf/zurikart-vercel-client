import { Theme } from "@mui/material/styles";

const styles = {
    StickyHeader: {
        bgcolor: "background.default",
        py: "12px"
    },
    NavBar: {
        py: "10px"
    }
}

export default styles;